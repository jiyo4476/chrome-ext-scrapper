{
  "name": "job-tracker-chrome-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chrome extension for saving visible job postings into the Job Tracker API.",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "compile": "tsc --noEmit",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest run",
    "quality": "npm run format:check && npm run lint && npm run compile && npm run test && npm run build",
    "postinstall": "wxt prepare"
  },
  "dependencies": {
    "zod": "^3.25.76"
  },
  "overrides": {
    "esbuild": "^0.28.1",
    "shell-quote": "^1.9.0",
    "tmp": "^0.2.7",
    "uuid": "^14.0.1"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.326",
    "eslint": "^10.6.0",
    "globals": "^17.7.0",
    "jsdom": "^29.1.1",
    "prettier": "^3.9.4",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.63.0",
    "vitest": "^3.2.4",
    "wxt": "^0.20.27"
  }
}
